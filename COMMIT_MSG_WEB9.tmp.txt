feat(comunidad): conectar donaciones reales via Stripe Connect en la pagina publica

El boton "Donar ahora" y el slider de la calculadora de impacto ahora hacen un cobro real:
POST /api/public/affiliates/{slug}/donations (companion en maalca-api, ver ese commit).
Mismo patron que el checkout de pedidos (CartDrawer.handleCardCheckout) -- si el afiliado
tiene Stripe Connect activo, redirige al Checkout hospedado; si no, cae al link de WhatsApp
existente (donateLink); si tampoco hay WhatsApp configurado, muestra "Donar -- proximamente"
deshabilitado como antes.

ImpactCalculator paso de manejar su propio estado de "amount" a ser un componente
controlado (amount/onAmountChange) -- el boton "Donar ahora" del padre necesita leer el
mismo monto que el slider para mandarlo al checkout.

"Recaudado este mes" ahora prefiere business.donationsRaisedThisMonth (suma real calculada
por el backend a partir de donaciones Paid, solo si el afiliado tiene Connect activo) sobre
el monto que el afiliado reporta a mano en Contenido -- cae al reportado a mano cuando el
afiliado todavia no tiene Connect activo, nunca muestra $0 falso.

registry.ts: nuevo campo opcional business.donationsRaisedThisMonth.
[slug]/page.tsx: nuevo fetch getDonationsSummary (mismo patron cacheado que
getCommunityMetrics, revalidate 60s), solo para businessType Community.

tsc --noEmit: 42 errores (baseline), 0 nuevos.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01R2QKsutmGsrVonxKk6wEQN
